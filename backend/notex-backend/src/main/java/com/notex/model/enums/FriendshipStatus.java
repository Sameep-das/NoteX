package com.notex.model.enums;

public enum FriendshipStatus {
    PENDING,   // Friend request sent, waiting for response
    ACCEPTED,  // Friends/Following
    BLOCKED    // User has blocked the other user
}
